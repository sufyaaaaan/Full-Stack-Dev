{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "description": "MERN Stack Ecommerce App Root",
  "scripts": {
    "start": "npx -y concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
